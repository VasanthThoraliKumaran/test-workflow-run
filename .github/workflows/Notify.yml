name: Notify

on:
  workflow_run:
    workflows: ["Build"]
    types:
      - completed

jobs:
  use-parent-output:
    runs-on: ubuntu-latest

    steps:
    - name: Run
      run: echo "Parent Workflow!"
